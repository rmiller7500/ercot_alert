app = "ercot-alert"
primary_region = "dfw"

[build]
  dockerfile = "Dockerfile"

[[services]]
  internal_port = 8000        # 👈 matches uvicorn
  protocol      = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443
